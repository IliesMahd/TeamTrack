import "../../styles/dashboard/dashboard.scss";
import NavBar from "../components/NavBar";
const Dashboard = () => {
  return (
    <main>
        <NavBar/>
        {/* A remplacer avec la navigation */}
        <div></div> 
    </main>
  );
};

export default Dashboard;
